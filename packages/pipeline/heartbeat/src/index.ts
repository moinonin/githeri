{
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src/heartbeat_package/__init__.py": "\"\"\"\nAdd model provider switching to run_pipeline.py - use Ollama (local) for spec generation\nand cloud provider (OpenRouter) for runbook scoring.\nAdd CLI flags --provider (ollama|openrouter) and --cloud-model for scoring,\nwith config in .env file.\"\"\"",
  "src