# Core pipeline deps (generation + validation + scoring)
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
PyYAML==6.0.3
requests==2.34.2
urllib3==2.7.0

# Env management
python-dotenv==1.0.1

# Fine-tuning deps (install separately on a GPU machine)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth
trl>=0.8.0
peft>=0.11.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
torch>=2.11.0

# Model export deps (GGUF conversion)
huggingface_hub>=0.23.0

# Evaluation deps (Ollama API already covered by requests)
