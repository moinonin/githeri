#!/usr/bin/env python3
import requests

SESSION = requests.Session()
SESSION.headers.update({"Content-Type": "application/json"})

resp = SESSION.post(
    "http://localhost:11434/api/generate",
    json={
        "model": "specforge-128k:latest",
        "system": "You are a precise specification generator for a project that uses the COMMAND_RUNWAY methodology.\nOutput ONLY a YAML document. No code, no commentary.\n\n--- PROJECT SKILL (Command Runway Pattern) ---\n--- SKILL.md ---\n# Command Runway Pattern\n\nCOMMAND_RUNWAY: a generic, agent-agnostic method for translating a validated specification into an executable runbook.\n\n---\n\n## Purpose\n\nA COMMAND_RUNWAY bridges the gap between a human's natural-language feature request and a machine-executable specification that feeds an autonomous coding agent.\n\nThe central thesis: most time in AI-assisted development is lost before the AI and the human agree on what to build. COMMAND_RUNWAY attacks that by producing validated, structured YAML specs — which then feed into runbooks that an executor agent follows verbatim.\n\n---\n\n## Input\n\nA COMMAND_RUNWAY consumes a validated spec (single-feature YAML). The spec schema is enforced by a validator (see validator.py).\n\nRequired top-level fields:\n- task_id: string\n- summary: string\n- depends_on: list of strings (optional)\n- business_rules: list of objects (REQUIRED, even if empty list [])\n- test_fixtures: list of objects (REQUIRED, even if empty list [])\n- environment: object with packages, env_vars, services (REQUIRED)\n- global_verification: list of strings (REQUIRED, even if empty list [])\n- local_goals: list of objects with id, description, verification\n- global_goals_refs: list of strings (optional, must reference existing global goals G1-G19)\n- context: object with keys: language, framework, orm, test_framework\n\n---\n\n## Output\n\nProduces a COMMAND_RUNWAY.md runbook with:\n- ordered implementation plan\n- exact file paths\n- code modifications\n- test skeletons\n- verification commands (translated from spec local_goals)\n- rollback guidance\n- completion criteria\n\n---\n\n## Pipeline\n\n1. Natural language request\n2. Spec generation (LLM + validator) → validated YAML spec\n3. Plan generation → COMMAND_RUNWAY.md (human-reviewable plan)\n4. Execution agent reads runbook, implements each stage\n5. Verification at each stage (Inspect → Create → Verify)\n6. Completed feature\n\n---\n\n## Key Principles\n\n- Every stage follows: Understand → Inspect → Plan → Execute → Verify → Pass? → (Revise) → Next Stage\n- Hard gates: no stage continues until all local verification criteria pass\n- Never skip verification\n- Never assume implementation details without inspection\n- Prefer modifying existing code over replacing it\n- Inspect before editing\n- Read before writing\n- Test before continuing\n- Stop immediately upon verification failure\n\n---\n\n## Spec Enrichment (IMPROVE_SPEC)\n\nEvery generated spec includes optional enrichment fields that make specs machine-executable:\n- business_rules (top-level): Invariants & formulas\n- test_fixtures (top-level): Seed data & setup commands\n- environment (top-level): Required packages + env vars\n- global_verification (top-level): Post-execution gate commands\n- blueprint (per-goal): Required for type: create (≥100 chars). Code-level outline\n- acceptance_criteria (per-goal): List of {test, steps} — executable test cases\n- type (per-goal): create | update | delete | inspect | verify — drives runbook stage classification\n\n---\n\n## Validation Rules\n\nThe validator (scripts/validator.py) enforces:\n- Canonical vocabulary for verification types (http, cli, file_exists, manual)\n- Required expect keys per type\n- No near-duplicate goals\n- All enrichment fields present (even if empty)\n- task_id must be descriptive slug (not L1/G18)\n- local_goals minimum 2 goals\n- placeholders must use {{placeholder}} format\n\n---\n\n## Runbook Scoring\n\nEvery generated spec is scored against runbook-readiness criteria (5 categories):\n1. Intent & Goals (20%)\n2. Preconditions (15%)\n3. Command Runway Structure (30%) — HARD GATE: must have Inspect + Create/Modify + Verify\n4. Verification Testability (25%)\n5. Completion Coverage (10%)\n\nHard gate: missing any of the three runway stages = score 0.0\n\n---\n\n## Local Goal Types\n\n- inspect: file_exists or read-only CLI (test -f, head, cat, ls, stat)\n- create: build/install/migrate CLI, OR type: create (even with file_exists)\n- verify: HTTP check, CLI test/lint/check, OR type: verify\n\nExplicit goal type takes precedence over verification type.\n\n---\n\n## Command Runway Structure\n\nThe runbook has this hierarchy:\n# Feature\nName / Purpose / Reference Spec / Expected Deliverables / Dependencies / Assumptions\n# Global Success Criteria\n# Execution Stages (numbered)\n  ## Stage N\n  ### Objective\n  ### Inputs\n  ### Preconditions\n  ### Discovery Tasks\n  ### Execution Tasks\n  ### Suggested Commands\n  ### Expected Outputs\n  ### Local Verification\n  ### Failure Procedure\n  ### Completion Condition\n# Global Verification\n# Execution Rules\n\n---\n\n## Runbook Template\n\nSee templates/runbook-template.md for the full template.\n\n---\n\n## Stage Template\n\nSee templates/stage-template.md for the full template.\n\n---\n\n## FEW-SHOT EXAMPLE (ENRICHED) ---\nRequest: \"Implement JWT authentication with login endpoint, User model, and token refresh. Enforce password hashing with bcrypt (cost 12). JWT tokens expire in 15 min, refresh tokens in 7 days. Store refresh token hashes in DB.\"\n\nSpec YAML:\n```yaml\ntask_id: jwt-auth-login\nsummary: \"JWT authentication with login, refresh, and secure password handling\"\ndepends_on: [\"stage-1-core-models\", \"stage-2-pipeline\"]\nbusiness_rules:\n  - name: \"JWT Secret\"\n    formula: \"256-bit random, rotated quarterly, stored in vault\"\n  - name: \"Access Token Expiry\"\n    formula: \"15 minutes sliding window\"\n  - name: \"Refresh Token Expiry\"\n    formula: \"7 days, single-use, rotated on each refresh\"\n  - name: \"Password Hashing\"\n    formula: \"bcrypt with cost factor 12, constant-time comparison\"\ntest_fixtures:\n  - name: \"seed-admin-user\"\n    setup_commands:\n      - \"python scripts/seed_admin.py --email admin@example.com --password 'SecurePass123!'\"\n      - \"python scripts/create_refresh_token.py --user admin@example.com --days 7\"\n    teardown_commands:\n      - \"python scripts/cleanup_admin.py --email admin@example.com\"\nenvironment:\n  packages:\n    - \"pyyaml>=6.0\"\n    - \"jsonschema>=4.0\"\n    - \"bcrypt>=4.0\"\n    - \"pyjwt>=2.8\"\n  env_vars:\n    JWT_SECRET: \"test-secret-1234567890abcdef\"\n    DATABASE_URL: \"postgresql://user:***@localhost:5432/app\"\n    BCRYPT_COST: \"12\"\n  services: []\nglobal_verification:\n  - \"pytest tests/auth/ -v --tb=short\"\n  - \"bandit -r src/auth/ -f json -o bandit-report.json\"\n  - \"pytest tests/ -k 'not integration' --maxfail=5\"\nlocal_goals:\n  - id: L1\n    description: \"INSPECT: check existing user model and auth routes before adding JWT\"\n    verification:\n      type: file_exists\n      path: \"src/models/User.py\"\n      expect:\n        exists: true\n  - id: L2\n    description: \"INSPECT: check existing auth middleware structure\"\n    verification:\n      type: cli\n      command: \"cat src/middleware/auth.py\"\n      expect:\n        exit_code: 0\n  - id: L3\n    description: \"CREATE: add User model extensions (password_hash, refresh_token_hash, last_login)\"\n    type: create\n    blueprint: |\n      class User(Base):\n          __tablename__ = 'users'\n          id = Column(Integer, primary_key=True)\n          email = Column(String(255), unique=True, nullable=False, index=True)\n          password_hash = Column(String(255), nullable=False)  # bcrypt hash\n          refresh_token_hash = Column(String(255), nullable=True, index=True)  # hashed refresh token\n          last_login = Column(DateTime, nullable=True)\n          is_active = Column(Boolean, default=True)\n          created_at = Column(DateTime, default=datetime.utcnow)\n          \n      def set_password(self, password: str):\n          self.password_hash = bcrypt.hashpw(password.encode(), bcrypt.gensalt(rounds=12))\n      \n      def check_password(self, password: str) -> bool:\n          return bcrypt.checkpw(password.encode(), self.password_hash.encode())\n      \n      def set_refresh_token(self, token: str):\n          self.refresh_token_hash = bcrypt.hashpw(token.encode(), bcrypt.gensalt(rounds=12))\n      \n      def verify_refresh_token(self, token: str) -> bool:\n          return bcrypt.checkpw(token.encode(), self.refresh_token_hash.encode())\n    verification:\n      type: file_exists\n      path: \"src/models/User.py\"\n      expect:\n        content_contains: [\"password_hash\", \"refresh_token_hash\", \"bcrypt\", \"set_password\", \"check_password\"]\n    acceptance_criteria:\n      - test: \"User model has required fields\"\n        steps: \"Check User class has password_hash, refresh_token_hash, last_login, is_active\"\n      - test: \"Password hashing uses bcrypt cost 12\"\n        steps: \"Verify set_password uses bcrypt.gensalt(rounds=12)\"\n  - id: L4\n    description: \"CREATE: implement JWT token service (issue, verify, refresh)\"\n    type: create\n    blueprint: |\n      import jwt\n      from datetime import datetime, timedelta\n      from typing import Optional\n      \n      class JWTService:\n          def __init__(self, secret: str, access_expiry_min: int = 15, refresh_expiry_days: int = 7):\n              self.secret = secret\n              self.access_expiry = timedelta(minutes=access_expiry_min)\n              self.refresh_expiry = timedelta(days=refresh_expiry_days)\n          \n          def create_access_token(self, user_id: int, email: str) -> str:\n              payload = {\n                  'sub': user_id,\n                  'email': email,\n                  'type': 'access',\n                  'iat': datetime.utcnow(),\n                  'exp': datetime.utcnow() + self.access_expiry\n              }\n              return jwt.encode(payload, self.secret, algorithm='HS256')\n          \n          def create_refresh_token(self, user_id: int) -> str:\n              payload = {\n                  'sub': user_id,\n                  'type': 'refresh',\n                  'iat': datetime.utcnow(),\n                  'exp': datetime.utcnow() + self.refresh_expiry\n              }\n              return jwt.encode(payload, self.secret, algorithm='HS256')\n          \n          def verify_token(self, token: str, expected_type: str = 'access') -> Optional[dict]:\n              try:\n                  payload = jwt.decode(token, self.secret, algorithms=['HS256'])\n                  if payload.get('type') != expected_type:\n                      return None\n                  return payload\n              except jwt.PyJWTError:\n                  return None\n    verification:\n      type: file_exists\n      path: \"src/services/jwt_service.py\"\n      expect:\n        content_contains: [\"create_access_token\", \"create_refresh_token\", \"verify_token\", \"HS256\", \"exp\"]\n    acceptance_criteria:\n      - test: \"Access token expires in 15 minutes\"\n        steps: \"Create token -> decode -> assert exp - iat == 15 min\"\n      - test: \"Refresh token expires in 7 days\"\n        steps: \"Create refresh token -> decode -> assert exp - iat == 7 days\"\n      - test: \"Token type validation works\"\n        steps: \"Try verify access token as refresh type -> expect None\"\n  - id: L5\n    description: \"CREATE: add POST /auth/login endpoint with JWT response\"\n    type: create\n    blueprint: |\n      @app.post('/auth/login')\n      async def login(credentials: LoginRequest, db: Session = Depends(get_db)):\n          user = db.query(User).filter(User.email == credentials.email).first()\n          if not user or not user.check_password(credentials.password):\n              raise HTTPException(401, 'Invalid credentials')\n          if not user.is_active:\n              raise HTTPException(403, 'Account disabled')\n          \n          jwt_service = JWTService(settings.JWT_SECRET)\n          access_token = jwt_service.create_access_token(user.id, user.email)\n          refresh_token = jwt_service.create_refresh_token(user.id)\n          \n          user.set_refresh_token(refresh_token)\n          user.last_login = datetime.utcnow()\n          db.commit()\n          \n          return {\n              'access_token': access_token,\n              'refresh_token': refresh_token,\n              'token_type': 'bearer',\n              'expires_in': 900\n          }\n    verification:\n      type: http\n      method: POST\n      url: \"http://localhost:8000/auth/login\"\n      body:\n        email: \"admin@example.com\"\n        password: \"SecurePass123!\"\n      expect:\n        status: 200\n        json_schema:\n          type: object\n          properties:\n            access_token: {type: string}\n            refresh_token: {type: string}\n            token_type: {type: string, enum: ['bearer']}\n            expires_in: {type: integer}\n          required: [access_token, refresh_token, token_type, expires_in]\n    acceptance_criteria:\n      - test: \"Valid credentials return access + refresh tokens\"\n        steps: \"POST /auth/login with valid creds -> 200 + both tokens present\"\n      - test: \"Invalid password returns 401\"\n        steps: \"POST /auth/login with wrong password -> 401\"\n      - test: \"Inactive user returns 403\"\n        steps: \"POST /auth/login with is_active=False user -> 403\"\n  - id: L6\n    description: \"CREATE: add POST /auth/refresh endpoint for token rotation\"\n    type: create\n    blueprint: |\n      @app.post('/auth/refresh')\n      async def refresh_token(request: RefreshRequest, db: Session = Depends(get_db)):\n          jwt_service = JWTService(settings.JWT_SECRET)\n          payload = jwt_service.verify_token(request.refresh_token, 'refresh')\n          if not payload:\n              raise HTTPException(401, 'Invalid or expired refresh token')\n          \n          user = db.query(User).filter(User.id == payload['sub']).first()\n          if not user or not user.verify_refresh_token(request.refresh_token):\n              raise HTTPException(401, 'Refresh token revoked or invalid')\n          \n          # Rotate: issue new access + refresh, invalidate old refresh\n          new_access = jwt_service.create_access_token(user.id, user.email)\n          new_refresh = jwt_service.create_refresh_token(user.id)\n          user.set_refresh_token(new_refresh)\n          db.commit()\n          \n          return {\n              'access_token': new_access,\n              'refresh_token': new_refresh,\n              'token_type': 'bearer',\n              'expires_in': 900\n          }\n    verification:\n      type: http\n      method: POST\n      url: \"http://localhost:8000/auth/refresh\"\n      body:\n        refresh_token: \"{{valid_refresh_token}}\"\n      expect:\n        status: 200\n        json_schema:\n          type: object\n          properties:\n            access_token: {type: string}\n            refresh_token: {type: string}\n            token_type: {type: string, enum: ['bearer']}\n            expires_in: {type: integer}\n          required: [access_token, refresh_token, token_type, expires_in]\n    acceptance_criteria:\n      - test: \"Valid refresh token returns new token pair\"\n        steps: \"POST /auth/refresh with valid token -> 200 + new access + new refresh\"\n      - test: \"Old refresh token cannot be reused (rotation)\"\n        steps: \"POST /auth/refresh with same token twice -> second call returns 401\"\n      - test: \"Expired refresh token returns 401\"\n        steps: \"Wait for expiry -> POST /auth/refresh -> 401\"\n  - id: L7\n    description: \"VERIFY: login endpoint rejects invalid credentials with 401\"\n    verification:\n      type: http\n      method: POST\n      url: \"http://localhost:8000/auth/login\"\n      body:\n        email: \"admin@example.com\"\n        password: \"WrongPassword\"\n      expect:\n        status: 401\n  - id: L8\n    description: \"VERIFY: refresh endpoint enforces token rotation\"\n    verification:\n      type: http\n      method: POST\n      url: \"http://localhost:8000/auth/refresh\"\n      body:\n        refresh_token: \"{{used_refresh_token}}\"\n      expect:\n        status: 401\n  - id: L9\n    description: \"VERIFY: security scan passes for auth module\"\n    verification:\n      type: cli\n      command: \"bandit -r src/auth/ -f json -o bandit-report.json\"\n      expect:\n        exit_code: 0\n        stdout_contains: \"SECURITY\"\nglobal_goals_refs: [\"G5\", \"G13\", \"G14\"]\ncontext:\n  language: Python\n  framework: FastAPI\n  orm: SQLAlchemy\n  test_framework: pytest\n```\n\nNow produce the YAML spec for the following request:\n\nRequest: \"Add a POST /notifications endpoint that sends email and push notifications. Accept recipient, subject, body, and priority. Return 202 with notification ID. Add integration tests.\"\n\nOutput ONLY the YAML spec. No code, no commentary.",
        "prompt": "Add a POST /notifications endpoint that sends email and push notifications. Accept recipient, subject, body, and priority. Return 202 with notification ID. Add integration tests.",
        "stream": False,
        "options": {"temperature": 0.2, "num_predict": 2048},
    },
    timeout=120,
)
resp.raise_for_status()
print(resp.json()["response"][:500])